# Config

The `config` directory is where all configuration files and classes reside. This includes Spring configuration classes that setup project-wide settings, bean configurations, and infrastructure-related setup such as database connections, security configurations, and any other framework or environment-specific setups.
